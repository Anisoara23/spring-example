<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:batch="http://www.springframework.org/schema/batch"
       xmlns:jdbc="http://www.springframework.org/schema/jdbc"

       xsi:schemaLocation="http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
http://www.springframework.org/schema/batch
http://www.springframework.org/schema/batch/spring-batch-2.2.xsd
http://www.springframework.org/schema/jdbc
http://www.springframework.org/schema/jdbc/spring-jdbc-3.2.xsd">

    <import resource="batch-reader-config.xml"/>
    <import resource="batch-writer-config.xml"/>
    <import resource="batch-processor-config.xml"/>

    <batch:job-repository id="jobRepository"/>
    
    <bean id="transactionManager"
          class="org.springframework.batch.support.transaction.ResourcelessTransactionManager"/>

    <jdbc:initialize-database>
        <jdbc:script location="schema/delete-schema.sql"/>
        <jdbc:script location="schema/schema.sql"/>
    </jdbc:initialize-database>

    <bean id="jobLauncher" class="org.springframework.batch.core.launch.support.SimpleJobLauncher">
        <property name="jobRepository" ref="jobRepository"/>
    </bean>

    <batch:job id="importBankFromCsvToDbJob">
        <batch:step id="importBankCsvToDbStep">
            <batch:tasklet>
                <batch:chunk reader="csvFileBankReader" writer="dbBankWriter" commit-interval="1000"/>
            </batch:tasklet>
        </batch:step>
    </batch:job>

    <batch:job id="importCustomerFromCsvToDbJob">
        <batch:step id="importCustomerFromCsvToDbStep" next="moveImportedCsvFileStep">
            <batch:tasklet>
                <batch:chunk reader="csvFileCustomerReader"
                             processor="compositeCustomerValidationProcessor"
                             writer="compositeDbCustomerWriter"
                             commit-interval="1000">
                </batch:chunk>
            </batch:tasklet>
        </batch:step>
        <batch:step id="moveImportedCsvFileStep">
            <batch:tasklet>
                <bean id="moveFileTasklet" class="org.example.batch.tasklet.MoveFileTasklet">
                    <property name="source">
                        <bean id="source" class="org.springframework.core.io.FileSystemResource" scope="step">
                            <constructor-arg value="#{jobParameters['source']}"/>
                        </bean>
                    </property>
                    <property name="destination">
                        <bean id="destination" class="org.springframework.core.io.FileSystemResource" scope="step">
                            <constructor-arg value="#{jobParameters['destination']}"/>
                        </bean>
                    </property>
                </bean>
            </batch:tasklet>
            <batch:listeners>
                <batch:listener>
                    <bean id="fileMoveStepListener" class="org.example.batch.listener.FileMoveStepListener"/>
                </batch:listener>
            </batch:listeners>
        </batch:step>
    </batch:job>

    <batch:job id="importCustomerFromDbToCsvJob">
        <batch:step id="importCustomerFromDbToCsvStep">
            <batch:tasklet>
                <batch:chunk reader="dbCustomerReader"
                             writer="csvCustomerWriter"
                             commit-interval="1000"/>
            </batch:tasklet>
        </batch:step>
    </batch:job>
</beans>